{
  "name": "normal-proxy",
  "version": "1.0.0",
  "description": "Standalone Node proxy compatible with Cloudflare Worker proxy format",
  "private": true,
  "scripts": {
    "start": "node normal-proxy.ts"
  }
}

